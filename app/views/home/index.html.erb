<h1>Сайт с Embedded Youtube video</h1>

<% @clips.each do |clip|  %>
    <div class="row">
     <p><%= clip.description %></p>
      <div class="embed-container">
        <%= embed(clip.source) %>
      </div>
    </div>
<% end %>
